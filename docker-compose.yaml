version: "3.9"

services:
  radio-bot:
    container_name: discord-radio-bot
    image: discord-radio-bot
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    restart: always
    ports:
      - "4033:4033"
    networks:
      - radiobot

networks:
  radiobot: